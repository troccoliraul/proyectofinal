export class Hss{
  id?: number;
  percent: number;
  title: string;
  subtitle: string;

  constructor(percent: number, title: string, subtitle:string) {
    this.percent = percent;
    this.title = title;
    this.subtitle = subtitle;

}
 }
